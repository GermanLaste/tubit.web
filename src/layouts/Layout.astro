---
// Cambia la importación al archivo que tiene las utilidades de la grilla
import '../global.css'; 
import Navbar from '../components/Navbar.jsx';

const { title } = Astro.props;
const image = "/banner.png";
---

<!doctype html>
<html lang="es" class="dark">
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="Servicio técnico de PC y Diseño Web en Villa Adelina" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <title>{title}</title>
        </head>
    
<body class="bg-[#050505] text-white min-h-screen flex flex-col font-sans selection:bg-purple-500/30 relative overflow-x-hidden">
            <div class="fixed inset-0 bg-grid-pattern -z-10 pointer-events-none"></div>
        
        <div class="fixed inset-0 bg-[radial-gradient(circle_farthest-side_at_50%_-20%,rgba(59,130,246,0.1),transparent)] -z-10"></div>

        <Navbar client:load />

        <main class="flex-grow pt-16">
            <slot />
        </main>

        <a href="https://wa.link/ic1rvt" ...>
          </a>
    </body>
</html>